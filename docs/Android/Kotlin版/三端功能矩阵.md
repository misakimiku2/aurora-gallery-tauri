# 三端功能矩阵

> 版本： v1（2026-08-31，现状列基于当日代码审计）
> 状态： 活文档，随功能开发持续更新
> 配套文档： [安卓Kotlin版并行开发规划](./安卓Kotlin版并行开发规划.md)（路线图与架构决策，稳定文档）｜[启动指南](./启动指南.md)（M0 分步执行）
> 用途： ①新功能三端取舍的登记处 ②Kotlin 版功能对齐清单（M7 验收依据）③平台差异的集中索引
> 说明： 「现状」列为当前 React 实现的实际状态（以代码为准，疏漏处以实际为准）；「Kotlin 目标」列为规划状态，括号内 M# 对应规划文档的里程碑。

## 目录

- [状态标记说明](#状态标记说明)
- [表 1 视图与浏览](#表-1-视图与浏览)
- [表 2 侧栏模块](#表-2-侧栏模块)
- [表 3 核心交互](#表-3-核心交互)
- [表 4 数据与智能](#表-4-数据与智能)
- [表 5 网络与互联](#表-5-网络与互联)
- [表 6 系统与设置](#表-6-系统与设置)
- [交互差异对照表](#交互差异对照表)
- [平台开关现状](#平台开关现状)
- [维护规则](#维护规则)

## 状态标记说明

| 标记 | 含义 |
|---|---|
| 完整 | 与桌面端功能一致 |
| 适配 | 功能保留，交互按触摸重设计 |
| 可用 | 现状能用，但仍是桌面式交互，待 Kotlin 版重做 |
| 隐藏 | 该端不提供（有意屏蔽） |
| 待定 | 未决策 |
| — | 不适用 |

---

## 表 1 视图与浏览

| 功能 | 桌面 | 平板（React 现状） | 平板（Kotlin 目标） | 手机（目标） | 备注 |
|---|---|---|---|---|---|
| 文件夹总览 folders-overview | 完整 | 完整（安卓默认入口 `__android_folders_root__`） | 完整（M2） | 待定（M8+） | 安卓端导航安全视图 |
| 文件浏览 browser | 完整 | 适配 | 完整（M1） | 待定（M8+） | 分组/瀑布流/列表；虚拟滚动；三档捏合列见 [表 3](#表-3-核心交互) |
| LAN 文件夹总览 lan-folders-overview | 完整 | 完整（含视频项） | 完整（M6） | 待定 | `__lan_root_images__` 置顶 |
| 标签总览 tags-overview | 完整 | 完整（长按替代右键） | 完整（M4） | 待定 | |
| 人物总览 people-overview | 完整 | 完整 | 完整（M4） | 待定 | |
| 专题总览 topics-overview | 完整 | 完整 | 完整（M4） | 待定 | |
| 画布 canvas | 完整编辑（PureRef 替代） | 可用（桌面式交互，无拖放导入） | **展示+轻整理（M5）** | 待定 | 双定位见规划 [§2.2](./安卓Kotlin版并行开发规划.md#22-canvas-双定位) |
| 搜索 | 完整 | 适配（收进菜单，展开式输入） | 完整（M1） | 待定 | |
| 排序/筛选/日期/标签过滤 | 完整 | 适配（全部菜单化） | 完整（M1–M2） | 待定 | |
| 多标签页 | 完整（含参考模式） | 完整 | 完整（M1） | 待定 | 关闭其他/关闭所有等操作同步对齐 |

## 表 2 侧栏模块

侧栏共六个 Section，三端全部保留（`src/components/TreeSidebar.tsx`）。

| 模块 | 桌面 | 平板（React 现状） | 平板（Kotlin 目标） | 手机（目标） | 备注 |
|---|---|---|---|---|---|
| 文件夹树 FolderSection | 完整（右键菜单、拖放） | 适配（禁右键、加大触点） | 完整（M2） | 抽屉收纳（M8+） | 排序/展开收起保留 |
| 专题入口 TopicSection | 完整 | 完整 | 完整（M2） | 抽屉（M8+） | |
| 网络 NetworkSection | 完整（含安卓设备列表） | 完整 | 完整（M6） | 抽屉（M8+） | 桌面↔安卓互联入口 |
| 人物 PeopleSection | 完整 | 完整 | 完整（M2） | 抽屉（M8+） | |
| 标签 TagSection | 完整 | 完整 | 完整（M2） | 抽屉（M8+） | |
| 画布入口 CanvasSection | 完整 | 完整（无平台守卫，安卓可用） | 完整（M2） | 待定 | 打开后进入 M5 的画布视图 |

## 表 3 核心交互

| 功能 | 桌面 | 平板（React 现状） | 平板（Kotlin 目标） | 手机（目标） | 备注 |
|---|---|---|---|---|---|
| 图片查看器 | 完整（React） | **原生**（NativeGalleryView，WindowManager 桥接） | 原生并入新壳（M3） | 原生组件复用（M8+） | 已完成原生化，见 [android-native-viewer.md](../android-native-viewer.md) |
| 幻灯片播放 | 完整 | 原生（SlideshowView 独立全屏层） | 原生并入（M3） | 复用（M8+） | |
| 图片比较器 ImageComparer | 完整 | 可用（桌面式交互） | 触摸化（M5） | 待定 | |
| 编辑模式/多选/范围选择 | 完整（Ctrl+点击） | 完整（长按进入选择模式） | 完整（M1） | 沿用（M8+） | |
| 删除/移动/复制 | 完整（回收站/任务） | 适配（删除确认、查看器内菜单） | 完整（M1/M4） | 待定 | 安卓删除有专门确认链路 |
| 拖拽（跨组件/拖入文件夹） | 完整 | **隐藏**（有意屏蔽） | 隐藏（画布内长按拖动除外，M5） | — | 触屏无对应交互，[规划 §2.4](./安卓Kotlin版并行开发规划.md#24-对齐线) 不对齐项 |
| 右键菜单 | 完整 | 长按菜单替代 | 长按菜单（M1 起） | 长按菜单 | 交互映射见[对照表](#交互差异对照表) |
| 捏合缩放三档 | —（滚轮/按钮调档） | 完整（小/中/大，逐档切换） | 完整（M1，GridLayoutManager spanSize） | 沿用三档设计（M8+） | `src/utils/androidThumbnailSizes.ts`；每手势至多一档 |
| 下拉刷新 | — | 完整 | 完整（M1） | 沿用（M8+） | |
| 全屏/沉浸模式 | 完整 | 完整（关面板+藏系统 UI） | 完整（M4） | 沿用（M8+） | 退出恢复面板与系统 UI |
| 返回手势链 | — | 完整（五级优先： 关菜单→退全屏→退编辑→取消选择→关标签） | 完整（M1） | 沿用（M8+） | |
| 面板开合 | hover/按钮 | 顶栏按钮（互斥开合） | 完整（M1–M2） | 抽屉式（M8+） | 推挤式同步动画，见设计偏好 |

## 表 4 数据与智能

数据层全部在 Rust + SQLite，三端共享；本表关注各端的管理与调用能力。

| 功能 | 桌面 | 平板（React 现状） | 平板（Kotlin 目标） | 手机（目标） | 备注 |
|---|---|---|---|---|---|
| 标签管理（打标签/增删/重命名） | 完整 | 完整 | 完整（M4） | 待定 | |
| 人物管理（重命名/创建/头像） | 完整 | 完整 | 完整（M4） | 待定 | |
| 专题管理 | 完整 | 完整 | 完整（M4） | 待定 | |
| AI 打标/描述/重命名 | 完整（TS 编排，`useAIAnalysis.ts`） | 可用（同一 TS 编排） | 完整（M6，编排下沉 Rust 后） | 待定 | 下沉项见规划 [§4-2](./安卓Kotlin版并行开发规划.md#4-逻辑下沉清单) |
| CLIP 语义搜索/以图搜图 | 完整（Rust 侧） | 可用（性能表现待实测） | 完整（M6） | 待定 | 已在 `src-tauri/src/clip/`，零成本继承 |
| 主色调提取/颜色搜索 | 完整 | 完整 | 完整（M6） | 待定 | |
| 元数据/右侧面板 | 完整 | 完整（与桌面同构） | 完整（M4） | 待定 | |

## 表 5 网络与互联

| 功能 | 桌面 | 平板（React 现状） | 平板（Kotlin 目标） | 手机（目标） | 备注 |
|---|---|---|---|---|---|
| LAN 服务端（对外共享） | 完整 | 隐藏（安卓为客户端模式） | 隐藏（维持客户端定位） | — | |
| LAN 客户端浏览 | 完整 | 完整（HTTP 直连 URL 缓存） | 完整（M6） | 待定 | 优化记录见 [安卓端局域网图片查看器性能优化全记录](../安卓端局域网图片查看器性能优化全记录.md) |
| LAN 上传 | 完整 | 完整 | 完整（M6） | 待定 | |
| 桌面浏览安卓设备（互联） | 完整（NetworkSection 的 androidDevices） | —（作为被浏览方） | 完整（M6，互联对等增强） | — | 核心逻辑在 Rust/HTTP 层，Kotlin 版直接继承 |
| 扫码连接 | — | 完整 | 完整（M6） | 沿用（M8+） | 见 [安卓端扫码连接功能实现记录](../安卓端扫码连接功能实现记录.md) |
| 标签/人物/专题跨端可见 | — | —（仅文件互联） | **阶段 1 在线查询（M6）** | 沿用 | 依赖内容指纹，策略见规划 [§5](./安卓Kotlin版并行开发规划.md#5-数据共享与互联)；离线同步默认不做 |

## 表 6 系统与设置

| 功能 | 桌面 | 平板（React 现状） | 平板（Kotlin 目标） | 手机（目标） | 备注 |
|---|---|---|---|---|---|
| 设置：通用面板 | 完整 | 适配（少量桌面专属项隐藏） | 完整（M4） | 待定 | GeneralPanel 两处桌面分支 |
| 设置：存储/扫描面板 | 完整 | 适配（安卓走 MediaStore 权限链） | 完整（M4） | 待定 | StoragePanel 桌面分支；存储管理器授权 |
| 设置：LAN 共享/AI 面板 | 完整 | 完整 | 完整（M4） | 待定 | |
| 系统分享 | —（剪贴板复制替代） | 完整（`androidShareImage(s)`） | 完整（M4） | 沿用（M8+） | |
| 任务进度通知 | — | 完整（`androidUpdateTaskNotification`） | 完整（M4） | 沿用（M8+） | |
| 状态栏/沉浸式控制 | — | 完整（`set_android_status_bar` / `set_android_immersive_mode`） | 完整（M4） | 沿用（M8+） | |
| 索引来源 | 磁盘文件系统 | MediaStore | 同现状（Rust 侧已分平台） | 同现状 | |
| 更新检查 | 完整 | 隐藏 | 待定（应用分发渠道再定） | 待定 | Tauri updater 在独立应用形态下不适用 |

---

## 交互差异对照表

桌面交互到触摸交互的标准映射，Kotlin 版按此实现：

| 桌面 | 安卓（平板/手机） |
|---|---|
| 右键菜单 | 长按菜单（毛玻璃样式沿用） |
| 拖拽 | 长按拖动（仅画布内）；跨组件拖拽无对应 |
| hover 高亮/悬停展开 | 按压态；面板只走按钮开合 |
| Ctrl+点击 多选 | 长按进入选择模式 |
| 滚轮缩放/滚动条 | 捏合三档/触摸滚动 + 下拉刷新 |
| Esc / 鼠标侧键 | 返回手势链（五级优先） |
| 双击标题栏等窗口操作 | 系统 WebView 全屏/沉浸替代 |

## 平台开关现状

**现状（债务）**： 平台判定散落在 30 余个文件，三种入口混用：

- `isAndroidPlatformCached()` / `isAndroidSync()`（`src/utils/androidPlatform.ts`）
- `_isAndroid`（`src/api/tauri-bridge/state.ts`）
- `isAndroidDevice = resourceRoot === 'android_media_store'`（App.tsx 等处派生）

问题： 没有单一位置能回答「安卓版到底有什么」，每个新功能要逐组件找开关；本矩阵 v1 的现状列就是靠逐文件审计拼出来的。

**目标（规划 M0）**： 平台差异集中声明，本矩阵条目与集中声明一一对应。届时本节改为集中声明文件的索引。

## 维护规则

1. **新功能先登记后实现**： 任何新功能动手前，先在本矩阵登记三端状态（桌面/平板现状、Kotlin 目标、手机目标），需要新里程碑的同步更新规划文档。
2. **落地即更新**： 功能上线后更新对应行的现状列；Kotlin 版推进时更新目标列的实际达成情况。
3. **里程碑引用**： Kotlin 目标列的 M# 编号与[规划文档 §6](./安卓Kotlin版并行开发规划.md#6-迁移路线图) 一一对应，M7 验收以「平板 Kotlin 目标」列全绿（完整/适配/隐藏符合规划，且无待定项）为准。
4. **不对齐项守则**： 新增「隐藏」必须写明理由（触屏无对应/桌面专属/有意取舍），避免变成「没来得及做」。
