# M1 应用骨架 + FileGrid 任务清单

> 版本： v4（2026-09-03 修订：① L8 终修——`applyCellWidth` 手改 `layoutParams.height` + `requestLayout` 会因「update 落在 layout 阶段」被 RV 吞掉，缩小换档后旧 item 封面高度停在上一档（真机确认），改用**带 payload 的局部刷新**（`notifyItemRangeChanged(0, itemCount, PAYLOAD_CELL)` + payload 绑定分支只改高度，不重载图片、不清 FLIP transform），真机验证通过；② L10 补齐——瀑布流实现 `MasonryPinchController` 跟手 FLIP（离线模拟列分配）、间距改用 `LayoutParams.spanIndex`、staggered 换档去掉二段式对齐网格路径、新增 `AuroraStaggeredLayoutManager` 手动预填解决露白/松手跳变；ADAPTIVE 跟手暂缓。遗留与接续点见 [§9](#9-遗留问题跨会话2026-09-02-会话结束) / [§9.1](#91-本会话技术要点备忘供下会话快速接续)）
> 版本： v3（2026-09-02 修订：捏合交互重构为**进度驱动 FLIP**（对齐三星系统相册手感），并修复一串 FLIP 相关 bug——requestLayout 被吞、post 化换档的重复触发、捏合露白、下半屏错乱、动画过快等。遗留与接续点见 [§9](#9-遗留问题跨会话2026-09-02-会话结束) / [§9.1](#91-本会话技术要点备忘供下会话快速接续)）
> 状态： 执行中（阶段 0/1/2.1~2.5 已完成，3.1 起待做；拆解自 [规划 §6](./安卓Kotlin版并行开发规划.md#6-迁移路线图) M1 里程碑）
> 配套： [规划](./安卓Kotlin版并行开发规划.md)（路线与架构）｜[矩阵](./三端功能矩阵.md)（功能对齐清单）｜[启动指南](./启动指南.md)（M0 已封版存档）
> 用途： M1 的小步执行清单。每完成一个小任务更新对应行的状态。

## 0. M1 目标与验收（来自规划 §6）

- **范围**：设计约定文档 + workspace 拆分（前置）；TopBar/TabBar/侧栏骨架；RecyclerView + GridLayoutManager；三档捏合；分组标题；编辑模式/多选/范围选择。
- **验收标准（两条硬指标）**：
  1. **单视图上限量级（1~2 万）**下滚动与捏合换档稳定 **60fps**（对照 React 版实测）。库总量（9.8 万）只验建库/启动链路，并入 1.2。
  2. 返回手势链（关菜单→退全屏→退编辑→取消选择→关标签）生效。
- **量级口径澄清（2026-09-01 验收人确认，替代原「9.8 万张滚动 60fps」）**：9.8 万是**图库总量**，不是单视图数据量——不存在一个页面同时展示 9.8 万张的场景。实测上限：最大文件夹约 1~2 万；最宽泛的颜色搜索结果 1 万+（且只回传路径数组，开销约为同量级图片列表的 1/5，见 [2.4](#24-虚拟滚动--大数据)）。原标准无法执行（造不出该场景），且会诱导为不存在的场景做分页设计，故改为按单视图上限验收。
- **额外目标（用户核心诉求）**：M1 结束时平板显示的是**平板上的真实图片（MediaStore）**，不再是 M0 的 `poc-data.db` 桌面子集。

## 1. 拆分原则

- **先数据后 UI**：先打通「MediaStore → Rust → UniFFI → Kotlin」的数据链路（对应「看平板图」诉求），再做网格与骨架。
- **每个任务一次提交**，提交信息带任务号（如 `M1-1.2: ...`），中断后从最后一个验收通过的任务恢复。
- **只新增不改旧**（延续 M0 纪律）：所有产物在 `kotlin-app/` 与 core crate 内新增；`src-tauri/`、`src/`、React 版一律不动，React 安卓版照常出货（dual-run）。

## 2. 依赖关系图

```
0.1 设计约定文档 ──┐
0.2 core 拆分 ─────┤
                   ▼
1.1 扫描架构决策 ── 1.2 扫描入库 ── 1.3 缩略图 ── 1.4 真图上屏
                                                    │
2.1 基础网格 ── 2.2 三档捏合 ── 2.3 分组标题 ── 2.4 大数据 ── 2.5 布局模式
                                                    │
3.1 状态模型 ── 3.2 TopBar ── 3.3 TabBar ── 3.4 侧栏 ── 3.5 面板开合
                                                    │
4.1 编辑/多选 ── 4.2 选择栏 ── 4.3 返回手势链 ── 4.4 下拉刷新
                                                    │
5.1 60fps 验收 ── 5.2 返回链验收 ── 5.3 捏合验收
```

（1.x 与 2.x 可部分并行；3.x 依赖 2.1；4.x 依赖 2.x 与 3.x）

---

## 阶段 0：前置（0.1 与 0.2 可并行）

### 0.1 设计约定文档
- **目标**：从桌面端提取设计 token，形成 Compose 主题实现参照，保证 Kotlin 版与桌面「一张脸」。
- **参考**：`tailwind.config.js`（surface token、动画）、`index.css`（581-595 颜色变量、圆角、毛玻璃、滚动条、安全区）。
- **交付**：`docs/Android/Kotlin版/设计约定.md`
- **验收**：颜色 token（含暗色）、间距、圆角、毛玻璃配方、字体层级、主色（收敛散落的 `#3b82f6`）齐全，可直接照做 Compose 主题。

### 0.2 workspace 拆分（core crate）
- **目标**：把 `lib.rs` 中平台无关的纯数据层抽出为独立 `core` crate，供 UniFFI 直调；tauri 壳只留薄适配层。
- **子任务**：
  - **0.2.1** core crate 骨架（`src-tauri/core/` 或独立 `core/`，Cargo.toml + mod）
  - **0.2.2** 搬移 `db/`（5 文件，0 tauri 依赖）
  - **0.2.3** 搬移 `color_db.rs`、`color_extractor.rs`、`file_types.rs`
  - **0.2.4** 搬移 `lan_share/` 平台无关部分（types/session/device_manager）
  - **0.2.5** State→参数适配：`db_commands.rs`、`color_search.rs` 等把 `tauri::State<X>` 改成 `&X`，tauri 薄壳负责取 pool 透传
  - **0.2.6** lib.rs 重接 core crate，`cargo build` 桌面 + 安卓 tauri 双通过
- **验收**：桌面与 React 安卓版功能零回归；core crate 内无任何 `tauri` 引用。

---

## 阶段 1：数据链路（看平板图的最小闭环，最高优先级）

### 1.1 MediaStore 扫描架构决策（需拍板）
- **问题**：独立 Kotlin 应用里，MediaStore 谁来扫？
  - **方案 A（推荐）**：Kotlin 端用 `ContentResolver` 直扫 MediaStore → 结果经 UniFFI 传给 Rust 入库。单向 `Kotlin→Rust`，复用 M0 已验证的管道，无 JNI 回调。
  - **方案 B**：保留 Rust→JNI→Kotlin 回调（复用现有 `media_store.rs` 的 `scan_device_all_via_kotlin`）。扫描编排在 Rust，但引入双向调用复杂度。
- **参考**：`src-tauri/src/android/media_store.rs`（`AndroidImageInfo`/`AndroidFolderInfo` 结构可复用为共享类型）。
- **验收**：决策写入本清单与规划 §7。

### 1.2 MediaStore 扫描 + 入库 Rust 函数
- **目标**：core crate 提供 UniFFI 导出的入库/查询函数（接收 Kotlin 扫出的原始列表，写 `file_index`，含去重与增量）。
- **参考**：`src-tauri/src/db/file_index.rs`（`batch_upsert`、`get_entries_under_path`）。
- **验收**：UniFFI 绑定生成，Kotlin 端能调；写入的文件夹/图片与 MediaStore 一致。

### 1.3 缩略图链路
- **目标**：MediaStore 缩略图（`MINI_KIND`）优先，不足再走 Rust 解码生成。
- **参考**：`src-tauri/src/android/thumbnail.rs`、`media_store.rs` 的 `get_android_system_thumbnail`。
- **验收**：网格能显示缩略图，不阻塞滚动。

### 1.4 真图上屏
- **目标**：Kotlin 扫 MediaStore → Rust 入库 → Rust 返回分组/列表 → Compose 显示。
- **验收**：平板显示的是**平板真图**，与 M0 桌面子集无关。

---

## 阶段 2：FileGrid 原生网格

### 2.1 基础网格
- **目标**：`RecyclerView + GridLayoutManager` 渲染图片网格（square 缩略图 + 选中态）。
- **参考**：`src/components/FileGrid.tsx`（网格渲染）、`src-tauri/gen/android/.../NativeGalleryView.kt`（现有 View 体系先例）。
- **验收**：能显示平板图片，滚动流畅。

### 2.2 三档捏合
- **目标**：`GridLayoutManager.spanCount` 动态换档，一次手势至多一档（沿用 M0 三档设计）。
- **参考**：`src/utils/androidThumbnailSizes.ts`（档位换算公式）、`src/hooks/usePinchZoom.ts`。
- **验收**：小/中/大 ≈ 9/6/4 列（平板），换档无跳变。

### 2.3 分组标题
- **目标**：按分组/排序规则渲染分组标题行（sticky）。
- **参考**：`src/components/FileGrid.tsx` 的 `GroupContent`；**分组规则本身在 `src/hooks/useFileSearch.ts` 的 `groupedFiles` memo**（FileGrid 只消费算好的 `FileGroup[]`）。
- **状态**：已完成（2026-09-01），实现记录见 §7。
- **验收**：分组标题与桌面端一致，重点比对三项——① 组标题文案（type = 大写 MIME 子类型如 `JPEG`，date = `YYYY-MM`）；② **组顺序**（按各组中**最早出现**的那张图在排序结果中的位置，不是字母序，也不是按组名排）；③ 折叠/展开交互与 sticky 吸顶。

### 2.4 虚拟滚动 / 大数据
- **结论（2026-09-01）：M1 不需要单独实现**。拆 2.4a / 2.4b 两条记录，均无待办。
- **2.4a 渲染层虚拟化 → 已由平台天然满足**：React 版做 `renderWindowRef` + `visibleItems`（±400px 过滤）是为自己造 ViewHolder 回收（React 无此机制，不窗口化则每帧滚动都要重渲染整个列表）；**RecyclerView 的 ViewHolder 回收就是这个机制**，且是框架层原生实现。滚 1954 张与滚 1~2 万张挂载的 view 数相同（仅视口内数十个）——这正是 2.1 实测「缩放到最小也非常流畅」的原因。**移植 React 版 renderWindow 属做无用功。**
- **2.4b 数据层分页 → 推迟到 M6，与 LAN 一起设计**：`list_images` 目前全量返回（`Vec<Image>`，5 字段，无 LIMIT/OFFSET），开销发生在**进文件夹的加载瞬间**（`Dispatchers.IO`），**不在滚动过程中**。按单视图上限 1~2 万粗估 0.2~0.5s，可接受；分页带来的游标管理、换档时的窗口失效、与 FLIP 锚点逻辑的交互，复杂度大于收益。
  - 量级参照：`Vec<Image>` 每张 5 个字段跨 JNI；颜色搜索走 `color_db` 内存缓存（keyset 分批预热，见 `refresh_cache_in_batches`）、只回传 `Vec<String>` 路径数组，1 万条约 1 万次转换。
  - LAN（M6）下单个文件夹同样是 1~2 万量级，分页价值从「避免卡死」降级为「改善首屏时间」（1~2 万条 JSON ≈ 1.5~3MB），随 M6 的 LAN 加载器一起定。
  - 已知非阻塞隐患（不修，备查）：`ThumbnailLoader.pregenerate` 对每张图 launch 一个协程 + 一次 `hdFile().exists()` 磁盘 stat，1~2 万时是全量磁盘扫描；1954 张实测无感知（`hdSemaphore` 限并发兜住），量级上来后再评估。
- **验收**：不适用（无待办）。若 M6 实现分页，验收并入 M6。

### 2.5 布局模式
- **目标**：grid / adaptive / masonry / list 至少先支持 grid（其余 M1 内按优先级补）。
- **参考**：`src/components/FileGrid.tsx` 的 `LayoutMode`；**排布算法在 `src/workers/layout.worker.ts`**（`viewMode === 'browser'` 分支，四种模式各自的公式都在那里）。
- **安卓端实际范围：三种，不含 list**。依据：`src/components/TopBar.tsx` 的 `isAndroid` 分支（1337-1350 行）用一个按钮在 `['grid', 'adaptive', 'masonry']` 之间循环，list 只出现在非安卓分支的完整菜单里（1368-1372 行）。**因此 Kotlin 端不实现列表模式**，`LayoutMode` 枚举只有三项。
- **状态**：已完成（2026-09-01），实现记录见 §7。
- **ADAPTIVE 为什么用「行容器」**：adaptive 需要精确控制每行放几张。GridLayoutManager 的换行由 span 累加自动决定，无法指定「这一行就放这 3 张」；StaggeredGridLayoutManager 是按列堆叠，没有「行」的概念。所以把**一行作为一个 item**（内部横向 LinearLayout 按算好的宽度摆图），配最简单的 `LinearLayoutManager`。
  - 代价：回收粒度从「图」变成「行」（一行 3~12 张，视宽高比而定），且换档是整行重排、无法复用现有 FLIP。
  - 行内单元格「只创建不销毁」（`ensureCells` 按最大需求补齐，多余的 GONE），避免每次 bind 重建 view。
- **验收**：与矩阵表 1「文件浏览」目标对齐；adaptive 重点验「行内等高、行间不等高、整行铺满、最后一行短时不拉伸」。

---

## 阶段 3：应用骨架

### 3.1 应用状态模型
- **目标**：Kotlin 化 `TabState`、`viewMode`、侧栏/面板可见状态、选中状态。
- **参考**：`src/types.ts`、`src/App.tsx` 的 state。
- **验收**：状态可支撑后续 TopBar/TabBar/侧栏。

### 3.2 TopBar
- **目标**：导航（返回/上级）、搜索框、排序/分组/视图切换、日期筛选。
- **参考**：`src/components/TopBar.tsx`。
- **验收**：与矩阵表 1「搜索/排序筛选」M1 目标对齐。

### 3.3 TabBar
- **目标**：多标签页切换/关闭/新建，标题/图标。
- **参考**：`src/components/TabBar.tsx`。
- **验收**：多标签页操作对齐（关闭其他/关闭所有）。

### 3.4 侧栏六 Section
- **目标**：Folder/Topic/Network/People/Tag/Canvas 六分区，互斥展开。
- **参考**：`src/components/TreeSidebar.tsx`。
- **验收**：六个 Section 齐全，排序/展开收起保留。

### 3.5 面板开合
- **目标**：顶栏按钮互斥开合侧栏/元数据面板（推挤式同步动画）。
- **验收**：与矩阵表 3「面板开合」目标对齐。

---

## 阶段 4：交互

### 4.1 编辑模式/多选/范围选择
- **目标**：长按进入选择模式；范围选择用 `lastSelectedId` 区间合并；框选（矩形相交）。
- **参考**：`src/hooks/useFileSelection.ts`、`useMarqueeSelection.ts`。
- **验收**：多选/范围/框选与桌面一致。

### 4.2 选择栏
- **目标**：底部操作栏（全选/取消、删除、分享、更多）。
- **参考**：`src/components/AndroidSelectionBar.tsx`。
- **验收**：按钮齐全，分享调系统分享。

### 4.3 返回手势链
- **目标**：五级优先——关菜单→退全屏→退编辑→取消选择→关标签。
- **参考**：`src/App.tsx` 的 `handleAndroidBackPress`（2281-2393 行）、`TopBar.tsx`（725-750 行）。
- **验收**：返回链逐级生效（规划 §6 验收二）。

### 4.4 下拉刷新
- **目标**：单指下拉触发扫描刷新，双指捏合时取消。
- **参考**：`src/hooks/usePullToRefresh.ts`。
- **验收**：下拉刷新可用。

---

## 阶段 5：性能验收

### 5.1 60fps 验收
- **目标**：9.8 万张图库滚动 + 捏合换档实测 60fps，录屏对照 React 版。
- **验收**：规划 §6 验收一达标，触发路线 A/B 重估（§3.4 条件二）。

### 5.2 返回链验收
- **目标**：五级返回逐级走查。
- **验收**：规划 §6 验收二达标。

### 5.3 捏合验收
- **目标**：三档逐档切换，不跟手、不跳档。
- **验收**：与矩阵表 3「捏合缩放三档」目标对齐。

---

## 6. 关键决策点（需验收人拍板）

| # | 决策 | 结论 | 说明 |
|---|---|---|---|
| D1 | MediaStore 扫描方向 | **A：Kotlin 直扫 → UniFFI 传 Rust 入库** | 已定（2026-08-31）：单向、无 JNI 回调，复用 M0 管道 |
| D2 | 执行优先级 | **阶段 0 前置先行** | 已定（2026-08-31）：先设计约定文档 + workspace 拆分，再进阶段 1 |
| D3 | 布局模式范围 | 待定 | 倾向先 grid，其余 M1 内按需补 |
| D4 | core crate 位置 | **仓库根 `core/`** | 与 ffi-poc/kotlin-app 并列，独立 workspace |

## 7. 任务状态记录

| 任务 | 状态 | 结果与备注 |
|---|---|---|
| 0.1 设计约定文档 | 完成 | 见 [设计约定.md](./设计约定.md)：颜色/间距/圆角/毛玻璃/字体/动画 token 已提取 |
| 0.2 workspace 拆分 | 完成 | `core/` crate 建立；`db`/`color_db`/`color_extractor`/`file_types` 共 8 文件 `git mv` 迁入；src-tauri 用 `pub use aurora_core::…` 重导出，壳内 `crate::db::` 引用零改动；桌面 build + 安卓 check 双通过；**桌面 `npm run tauri:dev` 运行时回归通过（2026-08-31 验收人确认）**；`lan_share` 平台无关部分延后 M6 |
| 1.1 扫描架构决策 | 完成 | 已定 D1=A：Kotlin 直扫 MediaStore → UniFFI 传 Rust 入库 |
| 1.2 扫描入库 | 完成 | core 新增 `ffi.rs`：UniFFI 导出 `initDb`/`upsertMediaImages`/`listFolders`/`listImages`；Kotlin 端 ContentResolver 扫 MediaStore → UniFFI 传 Rust 写 `file_index`（bucket 聚合 folder、content_uri 存 path 字段）；自定义 `AuroraError`（thiserror + `#[uniffi::Error]`）；绑定 `core/kotlin-bindings/uniffi/aurora_core/aurora_core.kt`，库名 `aurora_core`（→ `libaurora_core.so`） |
| 1.3 缩略图 | 完成 | 移除 Coil 直解 `content://`（三星 Tab S8+ 触发 `MediaRecoveryDatabase_Impl` 缺失）；两段式「先糊后清」：`loadFast`（内存 LruCache 128MB → 高清磁盘缓存 → MINI_KIND 立即上屏）→ 偏小/缺失时后台 `generateHd`（读 `content://` 原图字节 → UniFFI `generateThumbnail`：Rust `image` crate 解码缩放到 256px → 写高清缓存替换），Semaphore 限并发 2 缓解掉帧，`Log.d("AuroraKotlin")` 供 logcat 过滤；core 新增 `generate_thumbnail(data: Vec<u8>)` + `AuroraError::Thumbnail`，绑定/so 已重编；`compileDebugKotlin` 通过，真机验证待三星 Tab S8+ 确认 |
| 1.4 真图上屏 | 完成 | 平板显示真实 MediaStore bucket（如 `2025-01-27-Anis`、`747 Naho`、`Anis`），完全替换 M0 桌面子集；点进 bucket 显示对应图片数 |
| 2.1 基础网格 | 完成 | 先落 Compose `LazyVerticalGrid`，后因慢速拖动「一顿一顿」不跟手（对照系统相册/React 版均满帧），**最终改为原生 `RecyclerView + GridLayoutManager`（`AndroidView` 包裹）对齐系统相册性能基线**（用户明确「滑动流畅第一准则」）。新增 `ui/theme/AuroraTheme.kt`（设计约定 token：颜色/圆角/字体，主色收敛 `#3B82F6`）；`ui/components/RecyclerGrid.kt`（`SquareImageView` + `GridSpacingDecoration`）；重写 `ui/components/FileGrid.kt` 与 `FoldersOverview.kt` 为 `RecyclerView` 实现（`ImageAdapter`/`FolderAdapter`，代码构建 item 布局：square 缩略图 + 选中态边框/白勾角标 + 文件名 + 数量 badge）；缩略图复用 `ThumbnailLoader`（`peekMemory` 同步 + `loadFastLimited` 异步限并发，position 校验防错位）；`itemAnimator=null`、`clipToPadding=false`；build.gradle 加 `androidx.recyclerview:recyclerview:1.3.2`；`compileDebugKotlin` 通过 |
| 2.1+ 主视图 FoldersOverview | 完成 | 应用入口原为光秃秃 LazyColumn 列表，升级为对齐 React 版 `FoldersOverview.tsx` 的卡片网格主视图（用户优先要求）。新增 `ui/components/FoldersOverview.kt`：square 封面图（无封面时渐变 + 自绘 Folder 图标，material-icons-core 无 Folder）、右下角蓝色文件夹图标 + 图片数量角标、下方文件名；空态（无文件夹）展示 Folder 图标 + 文案。数据层配套：core `Folder` 结构扩展 `imageCount`/`coverUri`（封面取该文件夹最新一张图），`listFolders` SQL 加封面/数量子查询；UniFFI 绑定（`cargo run --bin uniffi-bindgen generate --library ...dll`）与 arm64-v8a so（`cargo ndk`）已重编并复制到 jniLibs；`compileDebugKotlin` 通过 |
| 2.2 三档捏合 | 完成 | 在原生 RecyclerView 基线上实现。`RecyclerGrid.kt` 新增：`targetCols`（对齐 React 版公式 w/150、w/220、w/330 → 平板约 9/6/4 列、手机 3/2/1 列）、`PinchGridSpanListener`（基于 `ScaleGestureDetector` 正确区分单指滚动/双指缩放，一次手势至多一档 `STEP_THRESHOLD=1.08` 切换后锁定防跳档）、`animateSpanChange`（FLIP 动画）；`GridSpacingDecoration.spanCount` 改 `var`；FileGrid / FoldersOverview 加 `level` state（默认中档）+ 捏合监听 + `AndroidView.update` 动态换档；列数用 dp 换算对齐 React 版 CSS 像素（三星 Tab S8+ 横屏约 1320dp → 9/6/4 列），`rv.width<=0` 时跳过防初始误算 2 列。**2026-09-01 补齐两个遗留（L1/L4）**：① FLIP 由「只动 Y + 300ms + `PathInterpolator(0.4,0,0.2,1)`」改为对齐 React 版 `FileGrid.tsx` 捏合分支的「**二维 translate(X,Y) + 240ms + `PathInterpolator(0.22,1,0.36,1)`**」，并修正锚点恢复（此前 `scrollToPositionWithOffset(anchorTop)` 未扣 `paddingTop` 与行间距 decoration，改为「按 offset 粗定位 → 布局后 `scrollBy` 精确修正」）；② 捏合误触发滚动：监听器自跟踪 pointer 数，第二指落下即 `stopScroll()` 并进入多指拦截态，多指降到单指时抑制到全部抬起，ACTION_UP 也消费（防失真 fling）。另加 `resetFlipTransform`（`onBindViewHolder` 清理复用残留 transform）与 `AuroraKotlin` 的 `[Pinch]`/`[FLIP]` 日志。`compileDebugKotlin` 通过；**待三星 Tab S8+ 真机验收（L3）** |
| 2.3 分组标题 | 完成 | 分组规则逐条对齐 React 版 `useFileSearch.ts` 的 `groupedFiles` memo：type 取 `format.toUpperCase()`、date 取 `createdAt` 的 `YYYY-MM`，空值归 `Unknown`；**组顺序 = 各组首个元素在已排序列表中的出现顺序**（React 依赖 `Object.entries` 的插入顺序），组内保持列表顺序。React 版的 `'size'` 选项未实现（会落到 `'Other'`），Kotlin 端同样不做。数据结构用 `sealed interface GridItem`（Header / Photo）拍平；标题占满整行靠 `GridLayoutManager.SpanSizeLookup`（`getSpanSize` 返回 `spanCount`）。sticky 用新增的 `StickyHeaderDecoration`（`onDrawOver` 自绘 + 离屏复用 header view，下一个标题顶上来时同步上推），锚点分组用**二分查找**定位——用倒序扫描在 1~2 万项时每帧要遍历上万次。支持点击标题折叠/展开（对齐 React 版 `collapsedGroups`）。数据层：`Image` 扩展 `size`/`createdAt`/`modifiedAt`/`format`（`upsert_media_images` 本已把 MediaStore 的 size/date_added/date_modified/mime 子类型写入 file_index，只是未暴露），`list_images` SQL 补 SELECT；UniFFI 绑定（`cargo run --bin uniffi-bindgen`）与 arm64-v8a so（`cargo ndk`）已重编。`compileDebugKotlin` 通过；**待三星 Tab S8+ 真机验收** |
| 2.4 虚拟滚动 / 大数据 | 不需要单独实现 | 2026-09-01 拆解结论（量级口径由验收人澄清，见 §0）：**2.4a 渲染层虚拟化**由 RecyclerView ViewHolder 回收天然满足——React 版的 `renderWindow`/`visibleItems` 是为补 React 缺失的回收机制，Kotlin 端不移植；**2.4b 数据层分页**推迟 M6——单视图上限 1~2 万，`list_images` 全量加载粗估 0.2~0.5s 且发生在进页瞬间（非滚动中），分页复杂度大于收益。详见 [§2.4](#24-虚拟滚动--大数据) |
| 2.5 布局模式 | 完成 | **三种模式：GRID / ADAPTIVE / MASONRY，不含 list**（对齐 React 版安卓端：`TopBar.tsx:1337-1350` 的 `isAndroid` 分支只在 `['grid','adaptive','masonry']` 间循环，list 仅出现在桌面端完整菜单）。算法对齐 `src/workers/layout.worker.ts`：GAP/PADDING 手机 10/8、平板 16/24。GRID = 正方形（沿用 2.1）；MASONRY = `StaggeredGridLayoutManager` + 封面高度 `colWidth / ratio` + 最短列堆叠；**ADAPTIVE = 行容器方案**——把「一行」作为 RecyclerView 的 item（内部横向 LinearLayout 按算好的宽度摆图），配 `LinearLayoutManager`，行化公式逐条对齐 React 版（累积 `w = targetHeight*ratio` 达宽成行 → `scale=(avail-gaps)/curW` 整行拉伸 → 最后一行不足容器一半宽时不拉伸），`targetHeight` = 该档位 thumbnailSize（对齐 `androidLevelToThumbnailSize`）。捏合换档：GRID 走 `animateSpanChange`，MASONRY 走新增的 `animateStaggeredSpanChange`（StaggeredGrid **没有** `scrollToPositionWithOffset`，改为 `scrollToPosition` 粗定位 + `scrollBy` 精确修正，多一层 `doOnLayout`），两者复用抽出的 `captureFlip`/`playFlip`/`fixAnchor`（二维位移 / 240ms / `PathInterpolator(0.22,1,0.36,1)`）；**ADAPTIVE 换档是整行重排、不做 FLIP**（行结构完全变化，与 React 版此处有差异）。**三种模式互相切换有 FLIP**：因 item 粒度不同（GRID/MASONRY 一个 item 是一张图，ADAPTIVE 一个 item 是一行），FLIP 以**图片 id** 为键而非 position，由 `adapter.forEachVisibleImage` 统一遍历两种 item 收集位置；锚点取最靠上的可见图，切换后 `scrollBy` 归位再做二维 FLIP，动画参数与捏合换档一致（240ms / `PathInterpolator(0.22,1,0.36,1)`）。两个前提：① 切换时**同步**换 LM + 提交新数据（只换 LM 会出现「新 LM + 旧数据」的中间帧）；② `submit` 加了内容相等的幂等守卫（否则 `LaunchedEffect` 的重复提交会重新 bind，把 FLIP 初始位移抹掉）。另：列宽变化必须走 `applyCellWidth` 而非 submit，否则换档后的一次全量刷新会把动画打断（详见 §9.1「FLIP 的头号天敌」）。容器宽度首帧用 `LocalConfiguration.screenWidthDp` 兜底，否则 adaptive 首帧会退化成「每张图独占一行」闪一下。临时切换入口 `ViewModeBar`（3.2 TopBar 完成后替换）。`compileDebugKotlin` 通过；**待真机验收**。**2026-09-03 修订（L8 收尾 + L10 补齐，见 §9）**：① 网格缩小后封面高度陈旧（长条）——`applyCellWidth` 改带 payload 局部刷新；② 瀑布流间距贴脸——decoration 对 Staggered 改用 `LayoutParams.spanIndex` 取真实列号；③ 瀑布流跟手——新增 `MasonryPinchController`（离线模拟列分配）；④ staggered 换档去掉二段式、对齐网格路径；⑤ 露白/松手跳变——新增 `AuroraStaggeredLayoutManager` 手动预填下方 1.5 屏；⑥ ADAPTIVE 换档改同步 submit + 按图片 id 的 FLIP。GRID 三问题真机确认解决，瀑布流跟手手感确认对齐网格，其余待真机验收 |
| 3.1 状态模型 | 未开始 | |
| 3.2 TopBar | 未开始 | |
| 3.3 TabBar | 未开始 | |
| 3.4 侧栏 | 未开始 | |
| 3.5 面板开合 | 未开始 | |
| 4.1 编辑/多选 | 未开始 | |
| 4.2 选择栏 | 未开始 | |
| 4.3 返回手势链 | 未开始 | |
| 4.4 下拉刷新 | 未开始 | |
| 5.1 60fps 验收 | 未开始 | |
| 5.2 返回链验收 | 未开始 | |
| 5.3 捏合验收 | 未开始 | |

---

## 8. 工程化记录（日常开发体验）

| 项 | 状态 | 结果与备注 |
|---|---|---|
| 一键开发命令 | 完成 | 新增 `npm run kotlin:dev`（检测设备 → `gradlew installDebug` → 启动 app → 实时日志，一条命令）与 `npm run kotlin:dev:so`（先 cargo-ndk 重编 so 再装包）；脚本 `scripts/kotlin-dev.ps1`，日志过滤 `AuroraKotlin`/`AndroidRuntime` 两 tag；另支持 `-LogOnly` 只看日志；对齐 React 版 `npm run tauri:android:dev:reuse` |

---

## 9. 遗留问题（跨会话，2026-09-03 会话结束）

> 下个会话接续时，先看本节 + [§9.1](#91-本会话技术要点备忘供下会话快速接续)，再决定从哪个问题入手。

| # | 问题 | 现状 | 下一步 |
|---|---|---|---|
| L1 | FLIP 动画与 React 版有差异 | **已关闭（09-01 修复，真机通过）**。差异 4 项：只动 Y 缺 X / 300ms→240ms / 缓动 `(0.4,0,0.2,1)`→`(0.22,1,0.36,1)` / 锚点漏扣 paddingTop 与行间距。参数出处 `src/components/FileGrid.tsx` 的 `flipAnimParamsRef` | — |
| L2 | 档位状态（level）未共享 | `FileGrid` / `FoldersOverview` 各自 `remember { mutableIntStateOf(1) }`，进文件夹 / 返回主界面时档位重置为中档 | 做 3.1 状态模型时一并把 level 提升到 App / MainActivity 层 |
| L3 | 三档捏合真机验收 | 已实现但未正式验收（尤其本轮改成「进度驱动」后，§5.3 的「一次手势一档、不跟手」旧描述已过时，验收标准要按新交互重写） | 对照 §5.3 + 矩阵表 3 走查，**并修订 §5.3 描述** |
| L4 | 双指捏合误触发滑动 | **已关闭（09-01 修复，真机通过）**。双注册 `setOnTouchListener` + `addOnItemTouchListener` + 自跟踪 pointer 数 | — |
| L5 | 捏合「布局成型时闪一下」 | **已修复（09-02，待真机验证）**。根因：换档曾放 `rv.post` → `lm.spanCount` 延迟更新导致同一帧重复触发（`(pending)` 打印两遍）+ retry 用 `rv.post` 不跨帧（3 次 retry 间隔 1~5ms 全挤在一帧，布局未刷新就 give up → 后续布局突现无过渡）。解法：换档改**同步** + retry 改**递归 doOnLayout 并在 draw 前补 `requestLayout`** | **重点真机验证**：松手后应是连贯过渡，不该再 `give up`、不该有 `(pending)` 重复 |
| L6 | 捏合进度手感（快慢 / 松手阈值） | 已实现但参数可能还需调。`PROGRESS_FULL_SCALE=1.5`（手指张合 50% 走完全程，值越大越慢）、松手 commit 阈值 `COMMIT_THRESHOLD=0.5`（进度过半即切换） | 真机感受快慢后微调这两个常量（在 `GridPinchFlip.kt`） |
| L7 | 捏合（从大变小）上下空白 | **已修复（09-02，待真机验证）**。根因：extra space 量不足。`calculateExtraLayoutSpace` 原给上 1/3、下 2/3 屏，但单次捏合只跨一档（4→6 或 6→9 列），卡片高度缩到约一半、列数增加一半，内容压缩约 2.2 倍，底部理论需约 1.2 屏才够。修法：底部 extra space 提到 1.5 屏（`height * 3 / 2`），顶部保持 1/3 屏（放大时上方只是被「挤」上去，无需额外空间）；并把 `AuroraGridLayoutManager` 从 `private` 提为 `internal`，`FoldersOverview` 也改用（原来用裸 `GridLayoutManager`，捏合时无 extra space、露白更严重） | 真机验证：从大档缩到小档全程无上下露白 |
| L8 | 操作后网格布局错乱 | **已关闭（09-03 真机确认）**。分两层：① **间距算法**（09-02 修，等间距 `left=gap*col/span`）——原「非首列加左间距」让首列内容宽比其余列大一个 gap，封面按统一 `cellPx` 设高后非正方形、捏合目标与真实列宽对不上；② **封面高度陈旧**（09-03 终修）——缩小换档后旧 item 是复用上来的（不重新 bind），`applyCellWidth` 原来手改 `layoutParams.height` + `requestLayout()`，但该 requestLayout 因「update 落在 layout 阶段」被 RV 吞掉，高度停在上一档（宽度缩小、高度不变 → 长条；滚走再滚回重新 bind 才恢复）。修法：改用**带 payload 的局部刷新** `notifyItemRangeChanged(0, itemCount, PAYLOAD_CELL)`，并新增 `onBindViewHolder(holder, position, payloads)` 分支只更新封面高度——不重载图片、不清 FLIP transform、不触发全量刷新 | — |
| L9 | 最大/最小档继续缩放仍有动画 | **已修复（09-02，待真机验证）**。根因：边界档位（level=0 或 2）继续缩放时 `targetLevel = (level + dir).coerceIn(0,2)` 被夹回当前档 → targetSpan == 当前 span，但 `progress` 仍 > 0，applyTo 仍算 transform；松手 `shouldCommit()` 为 true 走 commit 分支但 `level` 不变、`release()` 又不清 transform → 残留位移。修法：`onPinchProgress` 里 `targetLevel == currentLevel` 时强制 `progress=0`；`onPinchEnd` commit 分支加 `target != currentLevel` 判断（FileGrid / FoldersOverview 两处都已改） | 真机验证：最大档继续放大、最小档继续缩小，都**不该有任何画面变化** |
| L10 | MASONRY / ADAPTIVE 无跟手 | **MASONRY 已关闭（09-03，真机确认手感对齐网格）**。关键认知：瀑布流不等高但**列分配是确定性算法**（逐 item 放入最短列）+ 宽高比已知 → 可离线模拟出新档位每个 item 的目标位置，无需「整块缩放」（第一版做了整体 scale 预览，被验收人否定后移除）。配套修复了四个连带 bug：① 间距贴脸——`GridSpacingDecoration` 对 Staggered 必须用 `LayoutParams.spanIndex` 取真实列号（`position % spanCount` 在不等高时是错的，item 会落到最短列）；② 松手跳变/卡片甩飞——`animateStaggeredSpanChange` 原来的 `scrollToPosition + afterStableLayout` 二段式在连续捏合下旧动画被推迟数秒、对过期 snapshot 执行（drift 上千 px），已去掉二段式改用 `scrollToPositionWithOffset`（Staggered 实际有此 API）并同步执行 fixAnchor+playFlip；③ 锚点选择——不能用 `findFirstVisibleItemPositions().first()`（那只是列 0 的第一个可见），必须遍历取 top 最小的可见 item；④ 缩小露白 + 松手跳变——Staggered 没有 `calculateExtraLayoutSpace` hook（GRID 靠 `LinearLayoutManager` 的），新增 `AuroraStaggeredLayoutManager` 在换档布局后按同款分配规则手动预填下方 1.5 屏。**ADAPTIVE 仍未做跟手**（整行重排、图片会在行间迁移，预览需重跑行化并变换到行内 cell，复杂度高暂缓）——目前 ADAPTIVE 换档走「update 里检测档位变化 → 按图片 id 捕获快照 → 同步 submit → applyModeSwitchFlip」，有松手动画、锚点不跳，但无跟手预览 | L11 |
| L11 | 瀑布流预填方案的稳定性验证 | `AuroraStaggeredLayoutManager.prefillBelow` 用 `getViewForPosition + addView + layoutDecoratedWithMargins` 手动补布局，与 Staggered 内部的 lazy span 记录存在理论上的不同步（预填 view 的 spanIndex 未写入 mLazySpanLookup，滚动 fill 时会重新推算——推算规则与预填一致，理论自愈） | 真机重点验证：换档后**持续快速滚动**全列表，确认无错位/重复/跳动；若有异常优先怀疑预填分配与真实推算的偏差 |

### 9.1 本会话技术要点备忘（供下会话快速接续）

- **技术栈定型**：网格用原生 `RecyclerView + GridLayoutManager`（`AndroidView` 包裹），**不用** Compose `LazyVerticalGrid`（后者慢速拖动不跟手，见 2.1 记录）。缩略图复用 `ThumbnailLoader`（`peekMemory` 同步命中 + `loadFastLimited` 异步限并发）。
- **列数公式**：`targetCols(dp, level)` = `round(w/150)` 小 / `round(w/220)` 中 / `round(w/330)` 大，**宽度必须用 dp**（px 需除以 `density`，否则三星 Tab S8+ 上列数翻倍）。
- **捏合**：`PinchGridSpanListener` 基于 `ScaleGestureDetector`。**档位在松手时才切换**——`onScaleBegin`→`onPinchStart`、`onScale`→`onPinchProgress(scale, fx, fy)`、`onScaleEnd`→`onPinchEnd(scale)`（回调已从旧的 `onLevelChange` 改成这三个）。**拦截滚动不能只看 `isInProgress`**（slop 之前会漏事件），必须自跟踪 pointer 数：`ACTION_POINTER_DOWN` → `stopScroll()` + `multiTouch=true`；`ACTION_POINTER_UP`（`pointerCount<=2`）→ `suppressUntilUp=true`；`ACTION_UP/CANCEL` 先取 `blocking` 再清状态，把抬起事件也吃掉（防 RV 用失真 velocity fling）。
- **捏合监听器必须双注册**（最容易踩的坑，见 L4）：同一个实例同时 `setOnTouchListener(pinch)` 与 `addOnItemTouchListener(pinch)`。因为第一指落点决定分发路径——落在 item 上时 item 消费了 DOWN，只有 `OnItemTouchListener.onInterceptTouchEvent` 能提前拦（返回 true 会让 RV 调 `cancelTouch()`：置 IDLE + 停 fling + 给子 View 发 CANCEL 取消点击）；落在网格间隙上时没有子 View 消费，`OnItemTouchListener` 只在 ACTION_DOWN 被调一次，全程只有 `OnTouchListener` 可见。两条路径用 `(eventTime, action)` 去重防重复喂给 ScaleGestureDetector。
- **FLIP 现状**：`animateSpanChange` / `animateStaggeredSpanChange`（网格 / 瀑布流），流程拆成四段复用函数：`captureFlip`（记录旧 left/top **含当前 translation**，连续换档才不跳；同时清残留 transform 含 scale）→ 同步 `spanCount` 换档 + `scrollToPositionWithOffset(anchorTop - paddingTop)` 粗定位 → `runFlipWhenLayoutApplied`（用锚点宽度判据确认布局真刷新，draw 前补 `requestLayout` + 递归跨帧，见下）→ `fixAnchor`（`scrollBy` 精确修正）+ `playFlip`（二维反向位移 + 240ms `PathInterpolator(0.22,1,0.36,1)` 归位）。易错点：① `scrollToPositionWithOffset` 的 offset 语义是 `paddingTop + offset = 目标 decorated top`，与 `child.top` 差 padding + 行间距，必须二段修正；② X 方向不能漏（列数变化 x 也变）；③ 复用残留 transform 要在 `onBindViewHolder` 里 `resetFlipTransform` 清掉（**含 scale**，捏合后会残留）。
- **React 版 FLIP 两条路径不要混用**：捏合换档 = `duration 240 / cubic-bezier(0.22,1,0.36,1)`；面板开合（宽度变化）= `duration 300 / ease-out`。当前 Kotlin 版只实现前者。
- **数据量级口径（2026-09-01 验收人澄清，避免按错误指标设计）**：9.8 万是**图库总量**，不是单视图数据量，不存在「一个页面展示 9.8 万张」的场景。单视图上限约 1~2 万（最大文件夹）；最宽泛的颜色搜索 1 万+，且只回传 `Vec<String>` 路径数组（走 `color_db` 内存缓存 + keyset 分批预热），跨 JNI 开销约为同量级 `Vec<Image>` 的 1/5。`list_images` 全量加载的开销发生在**进页瞬间**（`Dispatchers.IO`）而非滚动中，所以「滚动流畅」与「数据量」基本无关——决定滚动性能的是 RecyclerView 回收，不是列表长度。
- **关键文件**：`kotlin-app/.../ui/components/RecyclerGrid.kt`（工具 + `PinchGridSpanListener` + FLIP 四段函数）、`GridPinchFlip.kt`（**进度驱动 FLIP 控制器**，09-02 新增）、`FileGrid.kt`、`FoldersOverview.kt`、`GridModels.kt`（LayoutMode/GroupBy/分组/格式化）、`GridItemViews.kt`（卡片/列表行/标题 view 构建 + `StickyHeaderDecoration`）、`MediaThumbnail.kt`、`ThumbnailLoader.kt`。
- **布局模式与 LayoutManager 的对应关系**：GRID + 分组标题 → `GridLayoutManager`（配合 `SpanSizeLookup` 让标题 `getSpanSize` 返回 `spanCount`）；MASONRY → `StaggeredGridLayoutManager`（**没有 SpanSizeLookup**，标题靠 `LayoutParams.isFullSpan = true`，必须写在 `onBindViewHolder` 里，写在 `onCreateViewHolder` 没用）；ADAPTIVE → `LinearLayoutManager` + 行容器。切换模式时整体替换 `rv.layoutManager`。
- **安卓端没有 list 模式**：`LayoutMode` 枚举只有 GRID / ADAPTIVE / MASONRY，依据是 `TopBar.tsx` 的 `isAndroid` 分支。若以后要加 list，别直接照抄 `layout.worker.ts` 的 browser+list 分支，先确认桌面端是否仍是唯一使用者。
- **adaptive 的两个坑**：① 行化依赖容器宽度，首帧宽度为 0 会退化成「每张图独占一行」，用 `LocalConfiguration.screenWidthDp` 兜底；② 行内单元格数量随宽高比变化（竖图多时一行可达十余张），用 `ensureCells` 只增不减地补齐，不要每次 bind 重建。
- **StaggeredGrid 换档的唯一差异**：没有 `scrollToPositionWithOffset`，只能 `scrollToPosition` 粗定位 → 等一次 layout → `scrollBy` 精确修正，所以 `animateStaggeredSpanChange` 比网格版多一层 `doOnLayout`。动画参数与网格版完全一致（共用 `captureFlip`/`playFlip`/`fixAnchor`）。
- **捏合 = FLIP 动画的进度条**（2026-09-02，对齐三星 Tab S8+ 系统相册手感）。**不是**「捏合中立即跳一档播固定动画」，也**不是**「整块网格跟手缩放」（第一版做错了，看起来像图片查看器）：
  - 捏合中：每个 item 从**旧档位的布局位置**插值移动到**新档位的布局位置**——就是 FLIP 该有的位移，但进度不靠时间推进，而由双指 `scale` 实时驱动（`progressFor(scale)`：scale 从 1 走到 1.08 正好对应进度 0→1，放大走下一档、缩小走上一档，方向可中途改变）。位置和尺寸一起插值，视觉上卡片连续滑向新格位。
  - 松手：`progress >= 0.5` 就落到新档位（`release()` 后真改 spanCount，transform 交给换档 FLIP 从当前进度收尾），否则 `settle()` 退回原档位。
  - **新档位的位置是纯算出来的**（列数、间距、文字区高度已知），捏合期间完全不触发布局，既流畅又绕开了下面那条「requestLayout 被吞」的坑。
  - **锚点**：以第一个可见 item 对齐，保证捏合前后它停在原处，网格不整体漂移。锚点在新布局的内容坐标通过 `anchorTop - anchorContentY` 换算成视口偏移，不需要知道 scrollY。
  - **只用于 GRID + 无分组**：MASONRY 行高不固定、ADAPTIVE 的「行」是按宽度堆叠的，都算不出新档位的精确位置；有分组时标题行占满整行、按网格公式算目标位置会错位。这些情况仍走「松手过阈值直接换一档 + FLIP」的常规路径。
  - **收尾动画按剩余进度缩短**：捏到 80% 松手只剩 20% 要跑，`flipDurationMs = 240 * (1 - progress)`（下限 80ms）。不缩短的话手感发黏——明明手指已经把动画推到八成了，松手却还要走满 240ms。
  - 边界：`anchorPos == NO_POSITION` 时**不启用**（`-1 / targetSpan` 会算出错误行号，导致整个网格偏移）。
  - **进度行程 ≠ 换档阈值**：`progressFor` 的行程用 `PROGRESS_FULL_SCALE = 1.5`（手指张开/收拢 50% 走完全程），不能用 `STEP_THRESHOLD = 1.08`——那是「触发换一次档」的阈值，拿它当行程会快到像瞬间跳档。
  - **捏合期间不触发布局 → 上下露白**：缩小（列数变多）时视口需要更多 item，但 RV 不会补充。解法是 `AuroraGridLayoutManager` 覆写 `calculateExtraLayoutSpace`，视口外多布局（上 1/3 屏、下 2/3 屏，取最大档位差估算）。
  - **连续换档会「下半屏全乱」**：换档是 post 出去的，期间可能又被更新的换档覆盖；旧的 `doOnLayout` 会拿早先的 snap 对着**已经被二次换档改掉的布局**算 delta，把卡片甩到错误位置。解法：playFlip 前检查 `lm.spanCount == newSpan`，不符就 `superseded` 放弃（视图切换同理检查 `appliedMode`）。
- **`AndroidView.update` 可能在 View 的 layout 阶段被触发**（2026-09-02 实踩，最难查的一个）：此时 `RecyclerView` 重写过的 `requestLayout()` 会把请求**吞掉**——`mInterceptRequestLayoutDepth > 0` 时只置 `mLayoutWasDefered` 而不调 `super.requestLayout()`。于是 `lm.spanCount = newSpan` 之后布局**不刷新**，FLIP 对着旧布局算 delta，全部 <1px 被跳过，表现是「换档偶发无动画，直接硬切」。日志特征：`children == oldCount` 且 `skipped` 全中（对照成功案例是 `children != oldCount`）。
  - **正确解法（两段，缺一不可）**：① 换档**必须同步**——`lm.spanCount = newSpan` 立即变化，同一帧后续的 update 才会因 `spanCount == newSpan` 直接 return，避免重复触发换档（曾经试过把换档 `rv.post` 出去，结果 `lm.spanCount` 延迟更新，同一帧多次 update 都以为还没换、打印两遍 `(pending)` 还各自 post，布局反而更乱）。② `runFlipWhenLayoutApplied` 用**锚点 item 宽度**做判据，没变就在 **draw 前补一次 `rv.requestLayout()` 并递归注册下一次 doOnLayout**（每帧一次）——这一步同时解决了两件事：同步换档那次 requestLayout 若因「update 落在 layout 阶段」被吞，这里补的一定生效（draw 前不在 layout 阶段）；递归保证跨帧，不会像 `rv.post` 那样三次 retry 挤在同一帧（间隔 1~5ms）全在布局刷新之前用完。
  - 别指望 `isLayoutRequested` 能判出来：布局没刷新时它同样是 false。
- **换 LayoutManager 会丢掉滚动位置**（2026-09-02 实踩）：视图切换时 `rv.layoutManager = 新LM` 后滚动位置丢失，新一屏图片与旧一屏完全没有交集，按图片 id 的 FLIP 一个都匹配不上。日志特征：`[FLIP-ModeSwitch]` 的 `missing` 全中。解法：切换后先 `adapter.indexOfImage(anchorId)` 找到锚点图所在的 item 下标（抹平 Photo 与 adaptive 行两种 item），`scrollToPosition` 粗定位，再 `scrollBy` 微调。
- **FLIP 的头号天敌是 `notifyDataSetChanged`**（2026-09-01 实踩）：FLIP 的初始位移设在 itemView 的 `translationX/Y` 上，而 `onBindViewHolder` 第一件事就是 `resetFlipTransform` 清掉它（防止复用错位）。所以**任何在初始位移设好后发生的全量刷新，都会把所有 item 重新 bind，动画当场消失，表现为硬切**。曾经一次回归正是这么来的——把 `cellWidthPx` 放进了 `LaunchedEffect` 的 key，换档必然改列宽 → 触发 submit → 全量刷新 → 捏合与视图切换的 FLIP 同时失效。
  - 规避：**列宽变化走 `adapter.applyCellWidth`（只改可见 item 的封面高度，不 notify）**，不进 `LaunchedEffect` 的 key；新 fill 的 item 在 bind 时自然拿到新值。
  - 另两个同源陷阱：① `applyCellWidth` 收到 0（adaptive 模式用不到列宽）时**不要置 0**，否则切回 GRID/MASONRY 会被误判成「首次量出宽度」而走全量刷新；② `decoration.spanCount` 赋值不会自动生效，必须配 `invalidateItemDecorations()`（换档在 `animateXxx` 内部已做，模式切换要另补）。
- **换档有两个触发来源，动画参数必须一致**：捏合（走 `animateSpanChange` / `animateStaggeredSpanChange`）与视图切换（走 `captureModeSwitch` / `applyModeSwitchFlip`），都复用 `FLIP_DURATION_MS` / `FLIP_INTERPOLATOR`。视图切换的 FLIP 以**图片 id** 为键（item 粒度在模式间不同），靠 `adapter.forEachVisibleImage` 抹平 Photo item 与 adaptive 行内 cell 的差异。
- **视图切换 FLIP 的两个前提**：① 切换时**同步**换 LM + 提交新数据——只换 LM 而数据还是旧 item 会出现「新 LM + 旧数据」的中间帧（adaptive 用 LinearLayoutManager 渲染一堆单图 item，整屏变成一列）；② `submit` 必须有内容相等的幂等守卫，否则 `LaunchedEffect` 随后的重复提交会重新 bind，`resetFlipTransform` 会把动画初始位移抹掉。
- **`scrollBy` 会同步平移所有 child**：`scrollBy(0, drift)` 后 `child.top` 变为 `newTop - drift`，所以 FLIP 的 dy 必须按 `oldTop - (newTop - drift)` 算，不能直接用收集到的 newTop。
- **sticky 标题的两个坑**：① RecyclerView 没有查询已挂载 ItemDecoration 的 API，挂载/移除要自己用 state 记账，否则切换分组方式会重复叠加；② 锚点分组必须**二分查找** header 位置表，倒序扫描在 1~2 万项时每帧要遍历上万次。
- **L7/L8 修复要点（2026-09-02，待真机验证）**：
  - **L7 露白**：`AuroraGridLayoutManager.calculateExtraLayoutSpace` 底部 extra space 从 2/3 屏提到 **1.5 屏**（单次捏合跨一档、内容压缩约 2.2 倍，底部需约 1.2 屏）。该类从 `private` 提为 `internal`，`FoldersOverview` 也改用（原来裸 `GridLayoutManager` 无 extra space）。
  - **L8 错乱**：`AndroidView.factory` 闭包只创建一次，不能直接捕获会变的量。目标列数在捏合回调里用 `rv.context.pxToDp(rv.width)` 现算（与 `update` 提交口径一致，避免屏宽兜底值 vs 实际宽度的 pxToDp 舍入差异）；`layoutMode`/`groupBy`/`gapPx` 用 `rememberUpdatedState` 读取（否则切换视图/分组后闭包读到旧值，非 GRID 被误当 GRID 走进度驱动）。
  - **L8 网格非正方形（长条）**：封面视图在 `buildPhotoView` 里是普通 `ImageView`，高度默认 WRAP_CONTENT。`cellWidthPx` 未量出时（首帧/复用残留）高度退化成图片本征宽高比 → 长条。修法：`SquareImageView` 改为「`layoutParams.height == WRAP_CONTENT` 时强制正方形、显式高度则尊重」——这样网格封面永远正方形、瀑布流/自适应行显式高度不受影响；`bindPhoto` 在 `cellWidthPx==0` 时回退 WRAP_CONTENT（避免复用带旧显式高度）。`textHeight = itemHeight - itemWidth` 依赖封面正方形，封面一旦变长条，`textHeight` 也算错，会连带捏合/FLIP 位置错乱。
  - **L8 捏合锚点错位（往上滚 + 松手跳位 + 网格错乱）**：`PinchFlipController.firstVisiblePosition` 原来取「top 最小」的 child，后改成 `top < height && bottom > 0`（raw 边界 + 0/height）——仍与 `LinearLayoutManager.findFirstVisibleItemPosition` 不一致：后者用的是「去装饰后的边界 + padding 之间的可视区」`decoratedTop < (height-paddingBottom) && decoratedBottom > paddingTop`。item 刚露头（raw bottom 落在 0~paddingTop 之间）时两边会差一行（实测 414 vs 423），导致捏合锚点与松手换档锚点不一致、`maxDelta` 巨大（152~676px）、网格错乱。**最终修法：`firstVisiblePosition` 直接委托给 `(rv.layoutManager as? LinearLayoutManager)?.findFirstVisibleItemPosition()`**，不自己实现可见性判据，与换档完全一致。
  - **L8 真正的根因（间距算法，2026-09-02 真机日志定位）**：`GridSpacingDecoration` 原用「非首列加左间距」，GridLayoutManager 却按 `inner/span` 给每列 chunk 宽度，于是**首列内容宽 = inner/span、其余列 = inner/span - gap**（实测 450 vs 415，差一个 gap）。而封面高、捏合目标列宽都按 `(inner-(span-1)gap)/span` 算（421），与真实列宽对不上 → 封面非正方形长条 + 捏合松手跳位。修法：`getItemOffsets` 改成**等间距** `left = gap*col/span`、`right = gap*(span-1-col)/span`，使每列内容宽度一致 = `(inner-(span-1)gap)/span`，与 `cellPx`/`newCellW` 完全吻合，封面正方形、捏合目标即真实布局（松手零变化）。这是 L8 的最终解，上面几条（SquareImageView、锚点对齐、闭包捕获）是配套/防御。
- **构建**：`cd kotlin-app && .\gradlew.bat :app:compileDebugKotlin`；改 Rust 后需 `cargo ndk` 重编 so + `uniffi-bindgen` 重生成绑定（两件事缺一不可，只改一边会在运行时因 FFI 字段不匹配崩溃）。完整流程见 `scripts/kotlin-dev.ps1 -So`（so）+ 手动 `cargo run --bin uniffi-bindgen generate --library target\debug\aurora_core.dll --language kotlin --out-dir ..\kotlin-app\app\src\main\java`。
- **2026-09-03 会话要点（L8 终修 + L10 补齐）**：
  - **「改 layoutParams + requestLayout」在 AndroidView.update 里不可靠**：`update` 落在 View 的 layout 阶段时 `requestLayout` 会被 RV 吞掉（L5 同源坑），**但 `layoutParams.height` 的修改已经生效**——于是出现「宽度按新档位、高度停旧档位」的半更新状态，且旧 item 是复用不重新 bind，会一直保持到滚走再滚回（用户实测「缩小后长条、拖离屏幕再滚回恢复」）。**可靠解法是带 payload 的局部刷新**：`notifyItemRangeChanged(0, itemCount, PAYLOAD_CELL)` + 重载 `onBindViewHolder(holder, position, payloads)`，payload 分支只改封面高度——不重载图片（避免闪）、不调 `resetFlipTransform`（FLIP 初始位移不被抹）、也不影响换档 FLIP（payload 重绑发生在 playFlip 之前）。
  - **Staggered 的列号必须用 `LayoutParams.spanIndex`**：瀑布流 item 不等高，会被放入最短列，真实列号 ≠ `position % spanCount`。decoration 若用后者算 left/right，列宽不一致、两张图贴在一起。满宽 header（`isFullSpan`）不加任何间距；首行（`position < span`）不加顶部间距。
  - **瀑布流换档对齐网格路径，不要二段式**：`scrollToPosition + afterStableLayout` 二段式有两个致命伤——① `scrollToPosition` 对不等高的 Staggered 定位极不可靠（日志实测锚点停在视口下方 1085px）；② `afterStableLayout` 排队后没有过期保护，连续捏合时旧动画被推迟数秒、对着全新布局执行（drift 上千 px、卡片甩飞、`animated=0`）。**Staggered 实际有 `scrollToPositionWithOffset`**（旧注释说没有是错的），粗定位并入换档 layout，`runFlipWhenLayoutApplied` 回调里**同步** `fixAnchor + playFlip`（superseded 保护），与 `animateSpanChange` 完全同构。
  - **锚点 = top 最小的可见 item**：不能用 `findFirstVisibleItemPositions().first()`——那只是「列 0 的第一个可见」，不等高时真正的顶部 item 可能在别的列，用错锚点 → fixAnchor 把页面滚到错误位置（松手跳位）。
  - **瀑布流跟手 = 离线模拟列分配**（`MasonryPinchController`，与 `PinchFlipController` 对称）：列分配是确定性算法（按 position 顺序放入最短列），宽高比已知（adapter 注入 `ratioAt`），模拟出 `docY[pos]/colOf[pos]`（含 decoration「列内每项 +gap」与满宽 header 规则）→ 每个 item 在旧/新位置间按 progress 插值（位移 + 等比缩放，pivot 左上角），锚点原地不动。模拟与真实布局的微小偏差由松手后的换档 FLIP 收尾吸收（captureFlip 记录的是带 translation 的视觉位置）。模拟结果缓存（span/可见上限不变不重算）。**整块 RV scale 的预览方案已彻底移除**——被验收人明确否定（整个容器像图片一样缩放，观感糟糕）。
  - **StaggeredGridLayoutManager 没有任何 extra layout space hook**（GRID 用的 `calculateExtraLayoutSpace` 属于 `LinearLayoutManager`）——这是瀑布流「缩小露白 + 松手跳变」的根源：布局只到视口边缘，捏合预览压缩内容时下方无 item；松手 fixAnchor 的 scrollBy 修正又在 draw 前瞬时补建一批无动画 item（观感=FLIP 结束帧与真实布局对不上）。解法 `AuroraStaggeredLayoutManager.prefillBelow`：换档布局后（span 缓存已失效的最佳时机）按同款「最短列优先」规则手动补建下方 1.5 屏——`getViewForPosition + addView + measureChildWithMargins + layoutDecoratedWithMargins`，列以 decorated left 识别、底边用 decoratedBottom（decoration 的 outRect.bottom/margin 恒为 0，故等于 child.bottom），满宽 header 占内容区全宽并统一更新各列底。预填 view 走正常 attach，后续滚动/布局由 Staggered 标准流程接管（风险见 L11，待真机滚动验证）。
  - **ADAPTIVE 换档改为同步 submit + 按图片 id 的 FLIP**：在 `update` 块检测档位 span 变化（`appliedAdaptiveSpan` state）→ `captureModeSwitch`（forEachVisibleImage 遍历行内 cell，天然支持 adaptive）→ **同步** `submit`（交给 LaunchedEffect 异步提交会丢滚动位置）→ `applyModeSwitchFlip`（锚点归位 + 二维 FLIP）。切到 ADAPTIVE 的模式切换帧同步记录 `appliedAdaptiveSpan`，防同一帧被换档分支二次触发。ADAPTIVE 仍无跟手预览（整行重排、图片在行间迁移，需重跑行化并变换到行内 cell，暂缓）。
  - **onPinchEnd 的 commit 逻辑 GRID/MASONRY 共用**：`shouldCommit() && target != currentLevel` 才落档，`flipDurationMs = 240 * remaining` 收尾；两套 controller（`pinchFlip` / `masonryPinch`）都 `release()`（保留 transform 供换档 FLIP 从当前进度收尾）。ADAPTIVE / 分组 GRID 仍走「过阈值直接换一档」。
