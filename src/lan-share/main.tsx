import React from 'react';
import ReactDOM from 'react-dom/client';
import LanShareApp from './LanShareApp';
import './lan-share.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LanShareApp />
  </React.StrictMode>
);
