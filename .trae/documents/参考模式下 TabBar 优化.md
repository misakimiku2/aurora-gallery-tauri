## 修改计划

### 文件: `src/components/TabBar.tsx`

#### 1. 隐藏非当前画布的标签页
- 修改标签页渲染逻辑，在参考模式下只渲染当前激活的标签页
- 保留标签页的基本样式和交互

#### 2. 隐藏关闭和新建标签页按钮
- 在参考模式下隐藏标签页上的关闭按钮（X）
- 隐藏底部的新建标签页按钮（+）

#### 3. "始终在前"按钮状态同步
- 添加 `useEffect` 监听 `isReferenceMode` 变化
- 当进入参考模式时，自动将 `isAlwaysOnTop` 状态设为 `true`
- 当退出参考模式时，恢复为 `false`
- 点击该按钮时可以切换窗口的始终置顶状态（与参考模式解耦）

#### 4. 清理不再需要的代码
- 移除之前用于调试的日志代码（如果有残留）

请确认这个计划后，我将开始实施具体的代码修改。