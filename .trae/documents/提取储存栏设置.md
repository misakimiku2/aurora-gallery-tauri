将"常规"栏中的存储相关选项（资源目录切换、缓存目录打开、数据备份）提取出来，单独创建一个新的"储存"栏，放在"常规"栏下方。

具体修改：
1. 在侧边栏"常规"按钮下方添加新的"储存"按钮（使用 Database 图标）
2. 创建新的 `state.settingsCategory === 'storage'` 内容区域，包含：
   - 资源目录切换
   - 缓存目录打开
   - 数据备份（导出/导入功能）
3. 从 `general` 分类中移除存储和数据备份相关内容

翻译键 `settings.catStorage` 已存在，无需额外修改。