## 任务概述
在 TabBar.tsx 的窗口控制按钮区域（最小化、最大化、关闭按钮旁边）添加一个"始终在前"按钮，点击后窗口将保持最前端运行。

## 实施步骤

### 1. 修改 TabBar.tsx

**导入部分：**
- 从 `lucide-react` 导入 `Pin` 图标（用于表示"始终在前"功能）

**状态管理：**
- 添加 `isAlwaysOnTop` 状态来跟踪窗口是否处于"始终在前"模式

**事件处理函数：**
- 添加 `handleAlwaysOnTop` 函数，使用 `getCurrentWindow().setAlwaysOnTop()` API 切换窗口的"始终在前"状态

**UI 部分：**
- 在窗口控制按钮区域（最小化按钮之前）添加一个新的按钮
- 按钮使用 `Pin` 图标
- 当窗口处于"始终在前"模式时，按钮显示激活状态（例如使用不同的颜色）
- 添加 tooltip 显示 "window.alwaysOnTop" 翻译键

### 2. 添加翻译键

需要在翻译文件中添加以下键：
- `window.alwaysOnTop`: "始终在前"

### 3. 代码变更详情

**TabBar.tsx 变更：**
1. 导入 `Pin` 图标
2. 添加 `const [isAlwaysOnTop, setIsAlwaysOnTop] = useState(false);`
3. 添加 `handleAlwaysOnTop` 异步函数
4. 在窗口控制按钮区域添加新按钮（位于最小化按钮之前）

### 4. 按钮样式
- 使用与其他窗口控制按钮一致的样式
- 激活状态下使用不同的颜色（例如蓝色或黄色）来指示"始终在前"已启用
- 使用 `Pin` 图标，当激活时可以旋转或改变颜色

请确认这个计划后，我将开始实施具体的代码修改。