## 修复计划

### 问题1：点击图片对比画布时图片被清空
**文件**: `src/components/ImageComparer.tsx`
**修改**: 修改 `handleMouseDown` 函数中的 `onSelect` 调用逻辑，当点击空白处时不调用 `onSelect('')`，或者确保 `onSelect` 不会清空 `selectedFileIds`。

具体修改：
- 在 `handleMouseDown` 函数中，当 `clickedId` 为 null（点击空白处）时，不调用 `onSelect?.('')`
- 或者修改条件，只在有意义的选择时才调用 `onSelect`

### 问题2：二级菜单鼠标悬停不稳定
**文件**: `src/components/ContextMenu.tsx`
**修改**: 改进二级菜单的悬停逻辑，添加延迟关闭机制或使用更稳定的交互方式。

具体修改：
- 添加 `setTimeout` 延迟关闭二级菜单
- 或者使用 CSS 的 `:hover` 伪类来替代 React 的 mouse 事件
- 确保二级菜单和父菜单项之间没有间隙

### 问题3：画布名称显示异常
**文件**: `src/hooks/useNavigation.ts`
**修改**: 在 `handleOpenCompareInNewTab` 函数中添加 `sessionName` 的生成逻辑。

具体修改：
- 在创建新标签页时，添加 `sessionName: generateCanvasName()`
- 需要复制 `generateCanvasName` 函数的逻辑或将其提取为共享工具函数

### 文件修改清单
1. `src/components/ImageComparer.tsx` - 修复点击清空问题
2. `src/components/ContextMenu.tsx` - 修复二级菜单悬停问题
3. `src/hooks/useNavigation.ts` - 修复画布名称生成问题