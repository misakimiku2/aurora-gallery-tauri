修改 `handleLoadSession` 函数，在读取 .aurora 文件成功后，使用用户选择的文件名（去掉扩展名）作为 `sessionName` 显示在顶部工具栏。

具体修改：
1. 在 `handleLoadSession` 函数中，获取用户选择的文件路径 `path`
2. 从路径中提取文件名（去掉 `.aurora` 扩展名）
3. 使用该文件名设置 `sessionName`，覆盖 manifest 中的 sessionName

修改位置：c:\Users\Misaki\Desktop\git\aurora-gallery-tauri\src\components\ImageComparer.tsx 第 1168-1298 行的 `handleLoadSession` 函数