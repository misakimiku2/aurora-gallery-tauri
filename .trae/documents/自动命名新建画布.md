修改 `App.tsx` 中的 `handleOpenCanvas` 函数，实现自动命名功能：

1. 在创建新画布时，检查所有现有的 tabs
2. 找出所有 `isCompareMode` 为 `true` 的 tab 的 `sessionName`
3. 解析出最大的画布编号（如"画布01"提取出数字 1）
4. 生成新的画布名称，格式为"画布XX"，其中 XX 是最大编号 + 1
5. 如果没有已有画布，则使用"画布01"

修改位置：c:\Users\Misaki\Desktop\git\aurora-gallery-tauri\src\App.tsx 第 2570-2591 行